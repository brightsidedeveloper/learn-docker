name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set Version Tag
        run: |
          export PATH=$PATH:/usr/bin
          kubectl get nodes
